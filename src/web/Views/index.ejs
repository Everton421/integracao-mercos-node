<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Painel de Sincronização - INTERSIG</title>

    <!-- Bootstrap CSS (v5.3.2) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    <!-- Opcional: jQuery e Bootstrap Datepicker (descomente se precisar) -->
    <!--
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/css/bootstrap-datepicker.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/js/bootstrap-datepicker.min.js"></script>
    -->

    <!-- Feather Icons -->
    <script src="https://cdn.jsdelivr.net/npm/feather-icons@4.28.0/dist/feather.min.js"></script>

    <!-- Estilos personalizados para o dashboard -->
    <link href="/css/dashboard.css" rel="stylesheet"> <!- Ajuste o caminho se necessário ->

  
</head>

<body>
    <header class="navbar navbar-dark sticky-top bg-dark flex-md-nowrap p-0 shadow">
        <a class="navbar-brand col-md-3 col-lg-2 me-0 px-3 fs-6" href="/">INTERSIG  </a>
        <button class="navbar-toggler position-absolute d-md-none collapsed" type="button" data-bs-toggle="collapse"
            data-bs-target="#sidebarMenu" aria-controls="sidebarMenu" aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-nav">
            <div class="nav-item text-nowrap">
                <a class="nav-link px-3" href="#">Sair (opcional)</a>
            </div>
        </div>
    </header>

    <div class="container-fluid">
        <div class="row">
           
   <%- include('components/sidebar', { activePage: 'home' }) %>
            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
                <div
                    class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                    <h1 class="h2" id="pageTitle">Painel Principal</h1>
                    <div class="btn-toolbar mb-2 mb-md-0">
                        <div class="btn-group me-2">
                            <button type="button" class="btn btn-sm btn-outline-secondary">Compartilhar (opcional)</button>
                            <button type="button" class="btn btn-sm btn-outline-secondary">Exportar (opcional)</button>
                        </div>
                    </div>
                </div>


                <h2>Status da Sincronização</h2>
                <p>Bem-vindo ao painel de sincronização INTERSIG. Selecione uma opção no menu lateral para gerenciar ou visualizar o status.</p>

                <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4 mb-4">
                    <!-- Card de Produtos -->
                    <div class="col">
                        <div class="card h-100">
                            <div class="card-body">
                                <h5 class="card-title"><span data-feather="package" class="me-2"></span>Produtos</h5>
                                <p class="card-text">
                                    Última sincronização preço: <strong id="statusProdutosPriceLastSync"> 'N/A'</strong><br>
                                    Última sincronização estoque: <strong id="statusProdutosStockLastSync"> 'N/A' </strong><br>
                                    
                                </p>
                                <!-- Formulário para acionar a sincronização -->
                                <form action="/produtos" method="get" class="d-inline">
                                    <button type="submit" class="btn btn-primary btn-sm">Sincronizar Agora</button>
                                </form>
                                    <a  href="/logs" class="btn btn-outline-secondary btn-sm ms-2">Ver Logs</a>
                            </div>
                            <div class="card-footer">
                                <small class="text-muted">Total Sincronizado: <span id="totalProdutos">  0  </span></small>
                            </div>
                        </div>
                    </div>

                    
                    <!-- Adicione mais cards para Serviços, Categorias, Pedidos, etc. seguindo o modelo acima -->
                    <!-- Exemplo Card de Pedidos -->
                    <div class="col">
                        <div class="card h-100">
                            <div class="card-body">
                                <h5 class="card-title"><span data-feather="shopping-cart" class="me-2"></span>Pedidos</h5>
                                <p class="card-text">
                                    Última sincronização: <strong id="statusPedidosLastSync">'N/A'</strong><br>

                                </p>
                                <form action="/pedidos" method="get" class="d-inline">
                                    <button type="submit" class="btn btn-primary btn-sm">Sincronizar Agora</button>
                                </form>
                                    <a  href="/logs" class="btn btn-outline-secondary btn-sm ms-2">Ver Logs</a>
                            </div>
                            <div class="card-footer">
                                <small class="text-muted">Total Sincronizado: <span id="totalPedidos">0</span></small>
                            </div>
                        </div>
                    </div>

                </div> <!-- Fim da .row dos cards -->

                <!-- Exemplo de uma tabela de logs recentes (opcional) -->
                <!--
                <h2>Logs Recentes</h2>
                <div class="table-responsive">
                    <table class="table table-striped table-sm">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Data/Hora</th>
                                <th scope="col">Tipo</th>
                                <th scope="col">Mensagem</th>
                                <th scope="col">Status</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (locals.recentLogs && recentLogs.length > 0) { %>
                                <% recentLogs.forEach(function(log, index) { %>
                                    <tr>
                                        <td><%= index + 1 %></td>
                                        <td><%= log.timestamp %></td>
                                        <td><%= log.type %></td>
                                        <td><%= log.message %></td>
                                        <td><span class="badge <%= log.success ? 'bg-success' : 'bg-danger' %>"><%= log.success ? 'Sucesso' : 'Erro' %></span></td>
                                    </tr>
                                <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="5" class="text-center">Nenhum log recente.</td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
                -->

            </main>
        </div>
    </div>

    <!-- Bootstrap Bundle JS (v5.3.2 - inclui Popper) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
    
    <!-- Scripts personalizados do dashboard -->
    <script src="/js/dashboard.js"></script> <!- Ajuste o caminho se necessário ->

    <script>
        // Ativar Feather Icons
        feather.replace();

        // Opcional: Inicializar Datepicker se estiver usando
        /*
        $(document).ready(function(){
            $('#datepicker').datepicker({
                format: 'dd/mm/yyyy',
                autoclose: true,
                todayHighlight: true
            });
        });
        */

        // Simples script para mudar o título da página principal com base no link da sidebar (exemplo)
        // Em uma aplicação real, você provavelmente renderizaria páginas diferentes ou usaria AJAX.
        document.querySelectorAll('#sidebarMenu .nav-link').forEach(link => {
            link.addEventListener('click', function(event) {
                // Remova 'active' de todos os links
                document.querySelectorAll('#sidebarMenu .nav-link').forEach(l => l.classList.remove('active'));
                // Adicione 'active' ao link clicado
                this.classList.add('active');
                
                // Atualiza o título da página (exemplo básico)
                // Em uma aplicação SPA ou com renderização no servidor por rota, isso seria diferente
                const pageTitle = document.getElementById('pageTitle');
                if (pageTitle) {
                    pageTitle.textContent = this.textContent.trim();
                }
            });
        });

    </script>
</body>
</html>